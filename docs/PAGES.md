# Page ownership

Which package serves each page, and where its data comes from. Generated by
reading the router at boot (`docker logs … | grep GIN-debug`) and attributing
every handler to its package, so it reflects what actually runs rather than
what was intended.

**231 routes. 109 are the host's**; the rest belong to eleven plugins.

Two rules explain most of the layout:

1. **A plugin owns a surface, not a table.** Several plugins run entirely on
   host-owned data through `Deps` — `lists` says so explicitly, and `tickets`
   ships no migration because the host creates its tables.
2. **The host owns anything that joins a USER to a release id.** Grabs and
   bookmarks both live here for the same reason: the users are the host's and
   no plugin owns both sides of that relation.

---

## Host-owned pages

| Page | Data |
|---|---|
| `/` | usenet index + plugin widgets (`SlotSiteWidget`) |
| `/browse`, `/search` | `pluginapi.UsenetIndex` + `catalog` for names |
| `/release/:id`, `/nzb/:id` | `UsenetIndex.ReleaseByID`; the NZB route WRITES `release_grab` |
| `/trending` | **`release_grab`** — see below |
| `/bookmarks` | **`release_bookmark`** — see below |
| `/calendar` | **event sources** — see below |
| `/achievements` | `rewards.achievements` extension |
| `/p/topics`, `/p/posts` | `forum_threads` / `forum_posts` (both in `public`) |
| `/groups` | `UsenetIndex.Groups` |
| `/u/:name` | users table + `SlotUserWidget` cards from plugins |
| `/stats` | usenet + catalog + forum counts |
| `/rules`, `/faq`, `/about`, `/staff` | static templates (no CMS — see UNIT3D-PARITY §5e) |
| `/api`, `/rss` | `pluginapi.UsenetNewznab` |
| `/sitemap.xml` | host, cached |
| `/admin`, `/admin/jobs`, `/admin/plugins`, `/admin/settings/*` | scheduler, runtime, plugin views |
| `/login`, `/register`, `/settings/*`, `/p/account`… | loon-baseline auth |

## Plugin-owned pages

| Plugin | Routes | Surface |
|---|---|---|
| `communities` | 20 | `/c/*` — sub-communities, join requests, invites |
| `forum` | 17 | `/community/forums/*`, `/admin/forum-categories` |
| `wiki` | 17 | `/wiki/*`, `/admin/wiki/*` |
| `donations` | 16 | `/help/donate`, `/admin/donate/*` (dev-flag gated) |
| `messages` | 12 | `/inbox/*`, `/admin/messages` |
| `playlists` | 9 | `/playlists/*` |
| `tickets` | 9 | `/support/*`, `/admin/tickets` |
| `news` | 7 | `/news/*`, `/admin/news` |
| `store` | 7 | `/store`, `/store/history`, `/admin/store` |
| `usenet` | 1 + views | `/admin/p/usenet` (view system), the crawl/build jobs |
| `rewards`, `dailyreward` | 1 each | claim endpoints + profile/home widgets |
| `catalog`, `scraper` | 0 | capabilities only — taxonomy, covers, metadata |

---

## The two that prompted this

### `/bookmarks` does NOT use the `lists` plugin

It is host code (`bookmarks_web.go`) over a host table:

    release_bookmark (user_id, release_id, created_at)
    UNIQUE (user_id, release_id)

A flat per-user set with one toggle, mirroring UNIT3D's `/users/x/bookmarks`.
No plugin is involved.

### `/trending` does NOT use `lists`, and there is no stats ingest

It is host code (`trending_web.go`) over `release_grab`, the table
`/nzb/:id` already writes on every download. Ranking is
`GROUP BY release_id ORDER BY count DESC` inside a window, then each id is
resolved back through `UsenetIndex.ReleaseByID`.

Nothing ingests or requests anything: the signal is a side effect of serving
downloads, which is why it needed no new collection path and no plugin.

### `/calendar` is a component, not a page about attendance

`calendar_web.go` owns the grid; it owns no data. Everything on it arrives
through a registered source:

    type calSource struct {
        Name string
        Fn   func(ctx, userID int64, from, to time.Time) []calEvent
    }

Two are wired today — daily-reward claims (via the `dailyreward.status`
extension) and followed releases (`release_bookmark` + `ReleaseByID`). Adding
subscription expiries or temporary-boost windows is a `calSource` in
`main.go`'s wiring and NO change to the page, the template or the CSS.

Events carry a range (`Start`/`End`), not a point, so a week-long boost is one
event over seven cells rather than seven events. The shared `{{template
"calendar"}}` partial lives in `site_chrome.html` — both parse sets — so a
plugin fragment can render the same grid without shipping its own.

Attendance is DERIVED, not stored per day: the plugin keeps one row per member
(last claim + streak length), so the calendar draws the run that is still live
and claims nothing about earlier ones.

---

## Open: one collection surface — see `docs/LISTS-PROPOSAL.md`

`lists` (loon-plugins) is the richer surface and is **not wired** — zero routes
at boot. `playlists` is wired and duplicates it. `/bookmarks` is a third,
smaller version again.

The blocker is not the duplication, it is the direction of the dependency:
`lists` owns no tables, so wiring it costs the host three tables, their
migrations and 26 `Deps` functions. `playlists` costs 70 lines because it owns
its schema. Reusing the better plugin currently means MORE host code.

`LISTS-PROPOSAL.md` is the request that fixes it — self-persistence, a `kind`
column, and a system owner — plus which of the list-shaped ideas actually fit
(collections, bookmarks, grabs, trending) and which do not (friends/blocked,
news).
