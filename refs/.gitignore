# Working images, not source: references are pasted in during a session and the
# generated _match_ sheets are regenerated on every run.
*.png
*.jpg
*.jpeg
# Session state: the rectangle picked at /dev/compare, rewritten every time a
# new focus is saved. Useful for a loop, meaningless in history.
_focus.json
!README.md
